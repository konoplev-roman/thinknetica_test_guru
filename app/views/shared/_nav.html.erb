<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item"><%= link_to t('nav.root'), root_path, class: 'navbar-brand' %></li>
      <% if user_signed_in? && current_user.admin? %>
      <li class="nav-item"><%= link_to t('nav.admin'), admin_tests_path, class: 'nav-link' %></li>
      <% end %>
    </ul>

    <ul class="navbar-nav">
      <% if user_signed_in? %>
        <li class="nav-item"><%= link_to current_user.first_name, edit_user_registration_path, class: 'nav-link' %></li>
        <li class="nav-item"><%= link_to t('nav.logout'), destroy_user_session_path, method: :delete, class: 'nav-link' %></li>
      <% else %>
        <li class="nav-item"><%= link_to t('nav.signup'), new_user_registration_path, class: 'nav-link' %></li>
        <li class="nav-item"><%= link_to t('nav.login'), new_user_session_path, class: 'nav-link' %></li>
      <% end %>
    </ul>
</nav>
