<% if resource.present? && resource.errors.present? %>
  <%= content_tag :div, class: 'resource-errors alert alert-danger', role: 'alert', data: { resource_id: resource.id } do %>
    <h4 class="alert-heading"><%= t('.header', count: resource.errors.count) %></h4>
    <hr>
    <% resource.errors.full_messages.each do |message| %>
      <p><%= message %></p>
    <% end %>
  <% end %>
<% end %>
