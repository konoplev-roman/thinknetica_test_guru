<tr>
  <td><%= link_to question.body, admin_question_path(question) %></td>
  <td class="text-right"><%= link_to 'Edit', edit_admin_question_path(question) %></td>
  <td class="text-right"><%= link_to 'Delete', admin_question_path(question), method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
